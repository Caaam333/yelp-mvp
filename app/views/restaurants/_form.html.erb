<%= simple_form_for(@restaurant) do |f| %>
  <%= f.input :name %>
  <%= f.input :address %>
  <%= f.input :category, collection: Restaurant::CATEGORY %>
  <%= f.submit class: "btn btn-primary" %> <%= link_to "Back", restaurants_path, class: "btn btn-danger ml-3" %>

<% end %>

